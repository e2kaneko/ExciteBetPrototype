version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step - Godot Web export is committed directly to /dist"
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
